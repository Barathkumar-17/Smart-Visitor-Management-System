export default function GuardPage() {
  return (
    <div className="panel">
      <h2>Guard</h2>
      <p className="muted">
        Gate entry, checkpoint and gate exit arrive in phases F1 to F3. Nothing on this
        screen calls the backend yet.
      </p>
    </div>
  );
}
