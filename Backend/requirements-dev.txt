# Test dependencies. Not needed to run the server - only to run the test suite.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
-r requirements.txt

pytest==9.1.1
httpx==0.28.1      # FastAPI's TestClient is built on it
